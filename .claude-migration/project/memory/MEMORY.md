- [BuenAdoptante theming system](project_theming.md) — palette remapping in globals.css under [data-theme] selectors, NOT Tailwind dark: variants; ThemeSelector labels ("Claro"/"Azul Noche") are NOT i18n'd
- [Drizzle migrations are hand-written](project_drizzle_migrations_handwritten.md) — hand-author drizzle/NNNN_*.sql; `drizzle-kit generate` hangs on stale meta snapshots despite CLAUDE.md listing it
- [Only themed Tailwind colors are theme-safe](feedback_themed_colors_only.md) — gradient stops, ring-*, hover variants, literal hex in <style>, and shades outside the [data-theme] rules render raw and break dark mode. Grep globals.css before adding a class.
- [SVG icons over emoji](feedback_svg_over_emoji.md) — for functional icons (info, close, affordances), always inline SVG with currentColor; emoji only OK as decorative subject markers next to text labels
- [Grep tests before deleting UI elements](feedback_grep_tests_before_deletion.md) — Playwright selectors aren't type-checked; missing one blocks the e2e job and stops the deploy. Update tests in the same commit as the UI removal.
- [Audit identity is at-a-glance info](feedback_audit_visibility.md) — on a vetting tool, `addedBy` and `sourceUrl` stay always visible on activity cards, never behind ··· / hover. The "hide footer" scannability lever doesn't apply to audit-identity fields.
- [Text-overflow audits need screen walkthroughs](feedback_overflow_audit_method.md) — grep-only audits miss JS-side truncation, deeply-nested flex children, and small-grid-tile patterns. Stress-test each user-facing screen with long values (50-char names, IANA timezones, multi-sentence notes).
- [E2E destructive-op tests need dedicated fixtures](feedback_e2e_test_isolation.md) — Playwright tests for merges/deletes must use fresh fixture rows (`test-*-fixture-*` IDs with INSERT OR REPLACE), never seed adopters. Targeting seed rows pollutes shared contactInfo and breaks downstream specs' strict-mode assertions.
- [Always read the pipeline-watch output file](feedback_pipeline_watch_output.md) — `gh run watch --exit-status` exit code is unreliable for failure detection. After any background pipeline-watch task completes, `tail` its output file and look for `FINAL: <conclusion>` before reporting status to the user.
- [Feature flag plumbing is 5 places for client-visible UI](feedback_feature_flag_5_place.md) — CLAUDE.md says 4 places, but if non-admin users see the gated UI, also add the key to `PUBLIC_FLAG_KEYS` in `/api/config/route.ts`. Otherwise the admin toggle is silently a no-op.
- [Public surface lives on its own domain](project_public_surface_separation.md) — showcase + form + contract must share ONE domain that is NOT buenadoptante.org; never propose merging into main Next app or splitting these three flows.
- [NEXT_PUBLIC_* / VITE_* are build-time inlines](project_buildtime_envvars.md) — Cloudflare runtime env vars don't reach build-prefixed vars; for per-env client URLs use a server resolver + /api endpoint + client hook (see contractUrl.ts pattern).
- [Pages production branch must match --branch](project_pages_production_branch.md) — `adoptions` project uses `master`, `adoptions-staging` uses `production`; deploying with the wrong --branch silently aliases as Preview, never Production.
- [adopter.status is deprecated, only avgRating is real](project_adopter_status_deprecated.md) — `adopter.status` is frozen at '5' default, no UI mutates it; show avgRating from history, never fall back to status. Two stale read sites still exist (applicants.ts:86, DuplicateMergeModal.tsx:156).
- [Batch pushes, don't drip](feedback_batch_pushes.md) — bundle related fixes in one commit; don't push every tiny change as its own version + CI cycle. Default to batching; only single-push when truly urgent or the user says "ship it".
- [PII feature ships as minor version 2.15.0](project_pii_minor_version.md) — the PII access-gating feature is a significant feature; release/tag it as minor `2.15.0`, not a `2.14.11-N` build suffix (user-authorized 2026-05-22).
- [PII address deserialize tests pending](project_pii_address_tests_pending.md) — round-trip tests for streetAndNumber/locality/raw deferred 2026-05-26; remind when next touching PII/address/contactEntries.
- [Collaborative vetting model](project_collaborative_vetting_model.md) — adds are open (activities, contact entries, aliases). Mutations gated: record-wide → owner+admin; per-contact-entry → owner ∨ admin ∨ original contributor. 'alias' type for same-phone-different-name. PII gates exposure, not contribution.
- [Read-only queries are pre-authorized](feedback_readonly_queries_pre_authorized.md) — don't pause to ask for permission on SELECT/PRAGMA/schema reads against prod or staging D1. Write operations still need explicit per-call approval.
